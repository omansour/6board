{% extends "::base.html.twig" %}

{% block body %}
<h1 class="page-header">Edit saved Search</h1>
<form action="{{ path('edit_filters', {id: entity.id}) }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}

    <button type="submit" class="btn btn-primary">Save</button>
    <a href="{{ path("browse_filters") }}" class="btn">Back</a>
</form>
{% endblock body %}
